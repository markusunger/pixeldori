/*
  The animation module can add and remove animations to frame buffers
*/

module.exports = (function animation() {
  let animations = [];

  return {
    addAnimation(type) {
  
    },

    clearAllAnimations() {
      animations = [];
    },

    animate: function animate(frame) {

    },
  };
}());
